export const initialState = {
    todoitem: "Let's learn reducers!",
    completed: false,
    id: 24601
  };

  export const titleReducer = (state, action) => {
      switch(action.type) {
          default: 
            return state;
      }
  }