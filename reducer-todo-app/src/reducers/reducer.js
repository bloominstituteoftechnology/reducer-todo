export const initialState = {
    todoitem: "Let's learn reducers!",
    completed: false,
    id: 24601
  };

  export const simpleReducer = (state, action) => {
      switch(action.type) {
          default: 
            return state;
      }
  }