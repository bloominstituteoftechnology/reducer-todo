export const initialState = {
    name: '',
    id: Date.now(),
    completed: false
}

export const reducer = (state, action) => {
    switch(action.type) {
        default:
            return state;
    }
}