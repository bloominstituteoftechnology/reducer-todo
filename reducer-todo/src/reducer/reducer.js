
const initialState = {
    todo:"Feed Dog",
    complete: false,
    id: Date.now()
}


const reducer = (state, action) =>{
         switch(action.type)
            case("ADD_TODO"):
                return (state):
            case("EDIT_TODO")
                return(state)
            case("DELETE_TODO"):
                return(state):
            default:
                return(state):




}

export default reducer;