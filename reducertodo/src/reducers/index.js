const initialState = [
    {
        item: 'learn useReducer',
        id: 5619815445651654456,
        completed: false,
    },
];

const reducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};
